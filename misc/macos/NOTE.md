### Key Accent Popup

If you're struggling with key accent popup, you can turn it off:

```sh
defaults write -g ApplePressAndHoldEnabled -bool false
```

And you also might want to lower the **Keyboard > Delay until repeat** setting.
